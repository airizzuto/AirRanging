import React from 'react';

const EmailVerified: React.FC = (): React.ReactElement => {
  return (
    <div>
      <p>Email verified. You can now proceed to login.</p>
      {/* TODO: Go back, Go to login */}
    </div>
  );
};

export default EmailVerified;
