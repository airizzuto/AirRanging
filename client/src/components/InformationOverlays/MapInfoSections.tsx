import React from 'react';

import Style from "./MapInfoSections.module.scss";

interface Props {
  // MapInfo: MapInfo;
}

const MapInfoSection: React.FC<Props> = () => {
  return (
    <div className={Style.Container}>
      {/* TODO: Info fields */}
    </div>
  );
};

export default MapInfoSection;
