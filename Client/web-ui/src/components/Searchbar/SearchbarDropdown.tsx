
/* React select documentation https://react-select.com/home */

// TODO: styling

const SearchbarDropdown = () => {

  // const promiseOptions = async (value: string): Promise<unknown> =>
  //   new Promise(resolve => {
  //     setTimeout(async () => {
  //       resolve(await searchFunction(value));
  //     }, 1000);
  // });

  return (
    <div>
      {/* <AsyncSelect
        cacheOptions
        loadOptions={pro}
        defaultOptions
      /> */}
    </div>
  );
};

export default SearchbarDropdown;
