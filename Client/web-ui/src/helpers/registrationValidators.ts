
export const isUserValid = (): boolean => {
  // TODO: user validation
  return false;
};

export const isEmailValid = (): boolean => {
  // TODO: email validation
  return false;
};

export const isPasswordValid = (): boolean => {
  // TODO: password validation
  return false;
};
