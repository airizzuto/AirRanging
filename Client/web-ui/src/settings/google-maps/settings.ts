import mapStyles from "./mapStyles";

export const containerStyle = {
  width: "100%",
  height: "100vh"
};

export const center = {
  lat: 0,
  lng: 0
};

export const options = {
  styles: mapStyles,
  disableDefaultUI: true,
  zoomControl: true
};
